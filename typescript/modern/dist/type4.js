"use strict";
function repeat(element, lenght) {
    const result = [];
    for (let i = 0; i < length; i++) {
        result.push(element);
    }
    return result;
}
console.log(repeat('a', 5));
console.log(repeat(1, 5));
